# 旅行記録アプリ (trip-diary) テスト計画書

## 改訂履歴

| 版 | 日付 | 改訂者 | 内容 |
|---|---|---|---|
| 0.1 | 2026-05-17 | hideharu-AI | 初版 — Phase 1 マージ後 / 計画 + 既存テスト棚卸し + セキュリティ自己監査と連動 |
| 0.2 | 2026-05-17 | hideharu-AI | §1 / §2 / §7 を Phase 3 実績に合わせて改訂 — E2E (Playwright) / 性能 (k6) / 静的解析 (rubocop + ESLint) / CI 4 jobs (ci.yml 3 + e2e.yml) を反映 |
| 0.3 | 2026-05-17 | hideharu-AI | §4 / §5 / §6 / §8 を Phase 3 実績に合わせて改訂 — §4 Phase 3 総数表記化 (Minitest 268 / Vitest 19 / E2E 1 / k6 2) / §5 E2E + k6 + CI 環境追記 + test DB init SQL (PR #89) / §6 E-M1/M2/M3/F-NOTIF 実績反映 + CLAUDE.md §12 連動 / §8 お気に入り / フォロー / 通知 / 検索 / ダークモード / F-LEAK-01 ガード追記 |

---

## 関連ドキュメント

- [要件定義書.md](./要件定義書.md) §4 非機能要件
- [ログ・監視・障害対応設計書.md](./ログ・監視・障害対応設計書.md) §5 段階導入 (カバレッジ目標と紐づく)
- [セキュリティ自己監査.md](./セキュリティ自己監査.md) (本書 §6 から参照)
- [学習ロードマップ.md](./学習ロードマップ.md)

---

## §1 目的とスコープ

- 「習う→慣れる→マスター」 ([学習ロードマップ.md](./学習ロードマップ.md) §1) の **慣れ** 段階で、**テストが品質の防波堤** であることを言語化する
- Phase 1 は基盤 + サンプルのみ → Phase 2 で本番品質に引き上げ → **Phase 3 で CI ゲート化 + 種別拡張** (実施済)
- **本書スコープ** (v0.2 で範囲拡大):
  - Backend Unit / Integration (Minitest)
  - Frontend Component / Composable (Vitest)
  - **E2E (Playwright Chromium smoke / WebKit / Mobile Safari)** ← v0.2 追加
  - **性能 (k6 Layer A: smoke + timeline SLO)** ← v0.2 追加
  - **静的解析 (rubocop-rails-omakase / @nuxt/eslint)** ← v0.2 追加
  - **CI 自動実行 (GitHub Actions ci.yml + e2e.yml + perf.yml)** ← v0.2 追加
  - 手動ブラックボックスチェックリスト (§8)
- **範囲外** (Phase 4 検討):
  - UI snapshot regression test (Playwright `toHaveScreenshot`)
  - 高 VU 負荷試験 (k6 Layer B 以降)
  - Web Push / リアルタイム通知 (ActionCable) の test
  - フロント a11y 自動検出 (axe-core 統合)

---

## §2 テストレベル

v0.2 で E2E / 性能 / 静的解析 行を追加。Phase 3 列は実装完了数を反映。

| レベル | ツール | 対象 | Phase 1 | Phase 2 | Phase 3 (現状) |
|---|---|---|---|---|---|
| Unit (Model) | Minitest | `User` / `Trip` / `Comment` / `Like` / `Follow` / `Notification` 他 validations | 1 件 | 全 model | **全 model + edge / 268 runs** |
| Unit (Service) | Minitest | `JsonWebToken` (encode/decode/expire) | 0 件 | 3 件 | 同左 |
| Integration (Controller) | Minitest IntegrationTest + `login_via_api` helper | `Api::V1::*` (auth / trips / comments / likes / favorites / follows / notifications 他) | 3 件 (auth) | + trips/comments/likes | **異常系含む / Notifications 17 件 + 既存** |
| Component (Vue) | Vitest | composables / pages | 1 件 (`useAuthStore`) | useApi / TripForm | **19 runs (auth / categories / darkMode / notifications)** |
| **E2E (browser)** | **Playwright** | smoke スペック (signup → login → trip → comment → like → logout) | — | — | **smoke 1 件 / Chromium で 5.9s GREEN (CI 上)** |
| **Performance (API)** | **k6** | smoke (全 endpoint 200 確認) / timeline (`GET /api/v1/trips` p95 SLO) | — | — | **2 scenarios / monthly cron + workflow_dispatch** |
| **Static Analysis (backend)** | **rubocop-rails-omakase** | 全 Ruby files | — | — | **0 offenses / 112 files / blocking CI** |
| **Static Analysis (frontend)** | **@nuxt/eslint** (Flat Config) | 全 Vue / JS files | — | — | **0 warnings / 4 errors remain (Issue #80 で解消予定) / continue-on-error** |

### CI 連携 (v0.2 追加)

| Workflow | trigger | jobs | 状態 |
|---|---|---|---|
| `.github/workflows/ci.yml` | push / PR (main) | backend (Minitest + rubocop + zeitwerk:check) / security (brakeman + bundle-audit + npm audit) / frontend (Vitest + build + ESLint) | **blocking** |
| `.github/workflows/e2e.yml` | push (main) / PR / workflow_dispatch | e2e (Playwright Chromium smoke / WebKit full on main) | **continue-on-error** (Issue #68 解決後に blocking 化予定) |
| `.github/workflows/perf.yml` | workflow_dispatch + 月次 cron | perf (k6 smoke / timeline + cleanup) | 手動 / 月次 (非ブロッキング) |

---

## §3 テスト技法

| 技法 | 本プロジェクトでの使い方 | 例 |
|---|---|---|
| **ホワイトボックス** | カバレッジ (SimpleCov branch) で未到達分岐を可視化 | `trip.rb` `images_size_within_limit` の break 経路 |
| **ブラックボックス (同値分割・境界値)** | バリデーション境界 | comment body 0/1/140/141 字 |
| **ブラックボックス (シナリオ)** | API シーケンス | login → trip 作成 → comment → logout |
| **グレーボックス** | レスポンス JSON 形を検証しつつ DB 状態も assert | like create 後 `likes_count` 増加 |
| **セキュリティテスト** | 自己監査と連動 (§6 参照) | timing attack / email 列挙 |

---

## §4 実施済みテスト一覧 (Phase 3 時点)

v0.3 で Phase 1 時点記述 (5 件) を Phase 3 実績 (Minitest 268 / Vitest 19 / E2E smoke 1 / k6 2 scenarios) に書き換え。代表テストと典拠ファイル群を示し、詳細は実コードを正とする。

### Backend (Minitest 268 runs)

| カテゴリ | 件数目安 | 典拠ディレクトリ | 代表テスト |
|---|---|---|---|
| Unit (Model) | 全 17 model | [`backend/test/models/`](../backend/test/models/) | `trip_test.rb` 画像枚数上限 / `notification_test.rb` 生成 hook / `revoked_jti_test.rb` JWT blacklist |
| Integration (Controller) | 全 15 controller | [`backend/test/controllers/api/v1/`](../backend/test/controllers/api/v1/) | `auth_controller_test.rb` (E-H1/E-H2) / `notifications_controller_test.rb` (17 件 / PR #74) / `trips_controller_test.rb` (異常系含む) |
| Unit (Service) | 3 件 | `backend/test/services/` 相当 | `JsonWebToken` encode/decode/expire |

### Frontend (Vitest 19 runs / 4 test files)

| ファイル | 件数 | 経緯 |
|---|---|---|
| [`useAuthStore.test.js`](../frontend/tests/composables/useAuthStore.test.js) | 1 | PR #4 サンプル |
| [`useCategories.test.js`](../frontend/tests/composables/useCategories.test.js) | 4 | カテゴリ絞り込み composable |
| [`useDarkMode.test.js`](../frontend/tests/composables/useDarkMode.test.js) | 7 | ダークモード切替 |
| [`useNotificationsStore.test.js`](../frontend/tests/composables/useNotificationsStore.test.js) | 7 | 通知センター store (PR #75) |

### E2E (Playwright Chromium smoke 1 件)

- [`e2e/tests/smoke.spec.js`](../e2e/tests/smoke.spec.js): signup → login → trip → comment → like → logout の一気通貫スモーク (CI Ubuntu Chromium で 5.9s GREEN / Mac local は Issue #68 で別途調査)

### 性能 (k6 Layer A / 2 scenarios)

- [`performance-tests/k6/scenarios/smoke.js`](../performance-tests/k6/scenarios/smoke.js): 全 endpoint 200 確認 + 5 VU × 1 分
- [`performance-tests/k6/scenarios/timeline.js`](../performance-tests/k6/scenarios/timeline.js): `GET /api/v1/trips` p95 SLO assertion
- 月次 cron + workflow_dispatch / cleanup は `bin/rails runner script/cleanup_test_users.rb` (PR #81)

### Phase 1 時点の歴史的サンプル (5 件 / リファレンスとして残置)

| # | 種別 | ファイル | テスト名 | 経緯 |
|---|---|---|---|---|
| 1 | Unit (Model) | `backend/test/models/user_test.rb` | `email is required` | PR #4 サンプル |
| 2 | Integration | `backend/test/controllers/api/v1/auth_controller_test.rb` | `signup error body is identical for email duplication and password too short` | PR #8 (E-H1 回帰) |
| 3 | Integration | 同上 | `login response time does not differ significantly for unknown vs known email` | PR #8 (E-H2 回帰) |
| 4 | Integration | 同上 | `login with correct credentials returns 200 and sets cookie` | PR #8 (正常系 baseline) |
| 5 | Component | `frontend/tests/composables/useAuthStore.test.js` | `login sets state.user from API response and marks fetched` | PR #4 サンプル |

> Phase 1 のテスト網羅率は意図的に低く保ち、Phase 2 で本番品質、Phase 3 で CI ゲート化 + 種別拡張という段階を踏んだ。実数は `bin/rails test` / `npm test -- --run` の出力 + SimpleCov の `coverage/index.html` を正とする。

---

## §5 テスト環境

v0.3 で E2E (Playwright) / 性能 (k6) / CI (GitHub Actions 4 workflow) を追記。Phase 1 時点の「CI 未導入」記述を実績に書き換え。

### Backend (Rails Minitest)

```bash
cd backend
bin/rails test
# 出力例: 268 runs, X assertions, 0 failures, 0 skips
```

- **fixtures**: BCrypt cost: 4 で高速化 ([test/fixtures/users.yml](../backend/test/fixtures/users.yml) のコメント参照)
- **認証ヘルパー**: `login_via_api(user, "password123")` で本物の login 経路を通る ([test/test_helper.rb](../backend/test/test_helper.rb))
- **カバレッジ**: SimpleCov で `coverage/index.html` 生成 (CI artifact retention 7 日)
- **test DB**: `db/init/01-grant-test-db.sql` で `trip_diary_test` 自動作成 + 'trip'@'%' 権限付与 (PR #89 / 新環境セットアップ自動化)

### Frontend (Vitest)

```bash
cd frontend
npm test -- --run       # CI 等価 (single run)
npm run test:coverage   # カバレッジ生成
```

- **環境**: happy-dom + @vue/test-utils + @pinia/testing
- **mock 戦略**: `useApi` は `globalThis.__apiMocks` 経由で共有 mock ([tests/setup.js](../frontend/tests/setup.js))

### E2E (Playwright) — Phase 3 追加

```bash
cd e2e
set -a && source ../.env && set +a
npm run e2e:smoke       # local 実行 (Issue #68 Mac local hydrate 問題あり)
```

- **ブラウザ**: Chromium (CI smoke / 5.9s GREEN) / WebKit (full on main push) / Mobile Safari (workflow_dispatch)
- **cleanup**: テスト後に `bin/rails runner script/cleanup_test_users.rb` で orphan 0 保証 (PR #81)
- **典拠**: [`e2e/tests/smoke.spec.js`](../e2e/tests/smoke.spec.js) / [`e2e/pages/`](../e2e/pages/) (Page Object Model)

### 性能 (k6) — Phase 3 追加

```bash
cd performance-tests
k6 run k6/scenarios/smoke.js
k6 run k6/scenarios/timeline.js
```

- **scenarios**: smoke (全 endpoint 200 確認) / timeline (`GET /api/v1/trips` p95 SLO assertion)
- **VU**: Layer A は 5 VU クランプ (本番影響なし)
- **trigger**: 月次 cron + `workflow_dispatch` (PR #71 / `.github/workflows/perf.yml`)
- **typhoeus**: k6 → Rails 通信は Cookie 認証 (E2E と同経路)
- **cleanup**: 各 scenario 末尾で `bin/rails runner script/cleanup_test_users.rb` を呼ぶ

### DB

- docker compose mysql (port 3316 / `mysql:8.0`)
- test は別 schema (`trip_diary_test` / init SQL で自動作成 / PR #89)

### CI — Phase 3 完了

| Workflow | trigger | jobs | 状態 |
|---|---|---|---|
| [`.github/workflows/ci.yml`](../.github/workflows/ci.yml) | push / PR (main) | backend (Minitest + rubocop + zeitwerk:check) / security (brakeman + bundle-audit + npm audit) / frontend (Vitest + build + **ESLint blocking**) | **blocking** |
| [`.github/workflows/e2e.yml`](../.github/workflows/e2e.yml) | push (main) / PR / workflow_dispatch | e2e (Playwright Chromium smoke / WebKit on main) | **continue-on-error** (Issue #68 解決後に blocking 化予定) |
| [`.github/workflows/perf.yml`](../.github/workflows/perf.yml) | workflow_dispatch + 月次 cron | perf (k6 smoke / timeline + cleanup) | 手動 / 月次 (非ブロッキング) |

> **lint 3 段階パターン** (CLAUDE.md §11-6): PR #77 rubocop / PR #79 ESLint grandfather / **PR #87 ESLint blocking 化完了** (Issue #80 解消)。

### カバレッジ目標 (ログ・監視・障害対応設計書 §5 と連動)

| Phase | line | branch | 実績 / 備考 |
|---|---|---|---|
| Phase 1 | 0% (許容) | 0% | PR #4 サンプル時 14.84% → PR #8 で auth 回帰 3 件追加して 35.9% |
| Phase 2 | ≥ 25% | ≥ 20% | **達成 (line coverage 92% / 全 controller の happy path + 異常系)** |
| Phase 3 | ≥ 50% | ≥ 40% | **達成 (Minitest 268 + Vitest 19 + CI 4 workflow blocking)** |
| Phase 4 (発展) | ≥ 70% | ≥ 60% | + UI snapshot regression / a11y / k6 Layer B 以降 (任意) |

---

## §6 セキュリティテスト方針

詳細は **[セキュリティ自己監査.md](./セキュリティ自己監査.md)** を参照。本書では「どこを Minitest / Vitest で守るか」「Phase 別実装ステータス」を記述する。v0.3 で Phase 2/3 列を実績反映。

| 項目 | 監査 ID | Phase 1 | Phase 2 | Phase 3 | 典拠 |
|---|---|---|---|---|---|
| signup の field-level エラー漏洩 | E-H1 | ✅ 回帰テスト | — | — | `auth_controller_test.rb` |
| login timing attack | E-H2 | ✅ 応答時間差テスト (許容差 200ms) | — | — | `auth_controller_test.rb` |
| dev seed の本番漏洩 | E-H3 | ✅ docs 強化のみ (実害なし) | — | — | `db/seeds.rb` ガード |
| login rate limit | E-M1 | — | ✅ **rack-attack 実装** | — | [`config/initializers/rack_attack.rb`](../backend/config/initializers/rack_attack.rb) |
| JWT blacklist (revoke) | E-M2 | — | ✅ **RevokedJti モデル + revoke endpoint** | — | [`app/models/revoked_jti.rb`](../backend/app/models/revoked_jti.rb) + `auth_controller#logout` |
| 横展開チェック (comment/like 冪等性・認可) | E-M6 | ✅ OK | — | — | `comments_controller_test.rb` / `likes_controller_test.rb` |
| 派生集計値の権限ガード (F-LEAK-01) | E-M3 | — | ✅ **PR #50 修正 + テスト** | — | `trips_controller_test.rb` / CLAUDE.md §12-5 |
| timezone (Asia/Tokyo 統一) | E-L1 | ✅ OK | — | — | `application.rb` + `docker-compose.yml` |
| signup race condition | E-L5 | — | 🟡 軽微 (DB unique index で代替) | — | `users` 表 unique constraint |
| パスワードリセット 5 原則 | E-PWReset | — | ❌ **未実装** | ❌ 未実装 | Phase 4 / MVP スコープ外 |
| Issue/PR テンプレのセキュリティチェック項目化 | E-Proc | ✅ 本 PR (PR #4 系) | — | — | [`.github/pull_request_template.md`](../.github/pull_request_template.md) |
| 通知センターの認可 (他人の notification 取得不可) | F-NOTIF-AUTHZ | — | — | ✅ **PR #74 integration test** | `notifications_controller_test.rb` (17 件) |
| 通知センターの mark_all_read 認可 (自分のみ) | F-NOTIF-MAR | — | — | ✅ **PR #74 integration test** | 同上 |

### CLAUDE.md §12 (Vue/Nuxt 規約) と連動するセキュリティ系規約

PR #89 (Closes #88) で CLAUDE.md §12 に追加された以下 4 規約は、レビュー時にセキュリティ系として突合する:

- **§12-5**: 権限ガードは派生集計値 (count / sum / avg) にも適用 (F-LEAK-01 再発防止)
- **§12-7**: Vue template `@click` で async action は handler 関数経由 (unhandled promise rejection によるエラー隠蔽防止)
- **§6-B**: テスト/perf データ削除は `bin/rails runner` 経由 (orphan 残留による fixture 干渉防止)
- **§6-G**: PR body / Issue 本文に修練城の禁止コマンド文字列を含めない (ハードウォール作動防止)

### 残課題 (Phase 4 候補)

- E-PWReset: パスワードリセット mailer + token (MVP スコープ外)
- 通知 cursor pagination (現状 50 件固定 / PR #74 R 残)
- Notification target 孤児化対応 (`dependent: :destroy` 追加 / PR #73 R 残)

---

## §7 今後の計画

### 実施済 (Phase 1-3 / v0.2 で reflect)

| 時期 | 内容 | 実績 |
|---|---|---|
| Phase 1 | Minitest / Vitest 基盤 + サンプル | 4 + 1 件 |
| Phase 2 | カバレッジ 25% を Definition of Done に追加 / Medium 残課題 (E-M1 / E-M2) 解消 | 達成 (line coverage 92%) |
| Phase 3 | カバレッジ 50% / CI でゲート化 / E2E + 性能 + 静的解析 拡張 | **達成** (Minitest 268 / Vitest 19 / E2E smoke / k6 / rubocop / ESLint / CI 4 workflow) |

### 残課題 (Pivot-6 〜 Phase 4)

| # | 内容 | Issue / PR | 優先度 |
|---|---|---|---|
| 1 | ESLint warning / error 0 化 + `continue-on-error` 削除 | Issue #80 | 中 (CI 体験改善) |
| 2 | E2E Playwright Chromium hydrate timing (local Mac 限定 / CI は GREEN) | Issue #68 | 低 (local debug 環境固有) |
| 3 | 通知 cursor pagination 実装 (現状 50 件固定) | 未起票 (PR #74 R 残) | 低 (MVP 制約) |
| 4 | Notification target 孤児化対応 (`dependent: :destroy` 追加) | 未起票 (PR #73 R 残) | 低 |
| 5 | k6 後続 (Layer B 以降 / Pivot-4 PR-H/I/J) — 任意 | 未起票 | 任意 |
| 6 | Web Push 通知 (F-NOTIF-03) / リアルタイム通知 (F-RT-01) | Phase 4 | 低 (MVP スコープ外) |
| 7 | UI snapshot regression / a11y 自動検出 | Phase 4 | 低 |
| 8 | 性能テスト Welch's t-test (timing assertion 強化) | Phase 4 | 低 |

### Pivot-6 (本番再デプロイ) 後の追加検証

- [ ] CloudFront URL に対する k6 smoke / timeline 実行 (5 VU クランプ確認)
- [ ] 本番環境での手動チェックリスト (§8) 全項目通過
- [ ] CloudWatch logs に重大エラーなし

### 改訂方針

- §1 / §2 / §7 は v0.2 (PR #86 / Closes #85) で Phase 3 実績に合わせて改訂済
- §4 / §5 / §6 / §8 は **v0.3 (本 PR / Closes #90) で Phase 3 実績に合わせて改訂済**
- 次回改訂は Pivot-6 (本番再デプロイ) 後の追加検証結果反映で v0.4 を予定

---

## §8 手動ブラックボックスチェックリスト (各 PR マージ前に実施)

E2E (Playwright Chromium smoke) は基盤に留まるため、**本リストが UI 回帰の最終防衛線**。Chrome で実施 (必要に応じて Safari)。v0.3 で Phase 2/3 追加機能 (お気に入り / フォロー / 通知 / 検索 / ダークモード / 派生集計値ガード) を追記。

### 認証フロー
- [ ] `/signup` で新規登録 → トップに遷移
- [ ] `/login` (正常) → 200 / トップに遷移 / ヘッダーに display_name 表示
- [ ] `/login` (誤 email) → 401 / 文言「メールアドレスまたはパスワードが間違っています」
- [ ] `/login` (誤 password) → 401 / **同じ文言** (email と区別されない / E-H1 確認)
- [ ] 未ログインで `/trips/new` → `/login` リダイレクト
- [ ] ログアウト → Cookie 削除 / `/me` が `{user: null}` / **RevokedJti 経由で再 login まで token 無効** (E-M2)

### Trip CRUD
- [ ] 一覧 `/` が public trip を新しい順に表示
- [ ] `/trips/new` で title/destination/期間/本文/出来事 (ネスト) 入力 → 作成 → 詳細遷移
- [ ] `/trips/:id/edit` で自分の trip 編集 → 反映
- [ ] **他人の trip の `/edit` を URL 直打ち → 403** (認可確認)
- [ ] 自分の trip 削除 → 一覧から消える

### コメント / いいね
- [ ] 詳細でコメント投稿 → リスト末尾に表示
- [ ] 他人のコメントに削除ボタンが**出ない**
- [ ] いいねボタンクリック → 数値+1 / 再クリック → -1 (2 回目以降の POST は 200 で冪等)

### お気に入り (Phase 2 / v0.3 追加)
- [ ] trip 詳細でお気に入りボタンクリック → 星表示 ON / `/me/favorites` に追加
- [ ] 同 trip で再度クリック → 星表示 OFF / `/me/favorites` から削除
- [ ] **他人の trip でも自分のお気に入りに追加可能** (公開 trip のみ / 非公開 trip は 404)
- [ ] お気に入り一覧でページネーション動作 (cursor-based)

### フォロー (Phase 2 / v0.3 追加)
- [ ] `/users/:id` でフォローボタンクリック → フォロワー数 +1 / フィード `?feed=following` に出現
- [ ] アンフォロー → フィードから消える
- [ ] **自分自身をフォロー不可** (UI でボタン非表示 + API で 422)
- [ ] フォロワー / フォロー中タブで一覧表示

### 通知センター (Phase 3 / PR #75 / v0.3 追加)
- [ ] 他ユーザーが自分の trip にコメント / いいね / フォロー → ヘッダー通知ベルに未読数バッジ表示
- [ ] ベルクリック → 通知一覧 dropdown 表示 (Headlessui Menu / `<Menu v-slot="{open}">` 経由 / §12-8)
- [ ] 「すべて既読化」ボタンクリック → handler 関数経由 / バッジ数 0 / トースト「既読化しました」 (§12-7)
- [ ] **他人の notification を ID で直接取得 → 403/404** (F-NOTIF-AUTHZ / `notifications_controller_test.rb`)
- [ ] 通知 50 件超過時の挙動 (現状 50 件固定 / cursor 未実装 / Phase 4 残)

### 検索 / 絞り込み (Phase 2 / v0.3 追加)
- [ ] トップで検索ボックス入力 → URL `?q=...` 同期 / リスト更新
- [ ] カテゴリチップクリック → URL `?category=domestic` 同期 / リスト更新
- [ ] サイドナビからカテゴリリンク (same-route) → ref 逆方向同期 watch (§12-1) 経由でリスト更新
- [ ] 並び替え `popular` → URL `?sort=popular` 同期 / `recent` 戻しで `?sort=` 削除 (PR #87 ESLint blocking 後の if-else 経路)
- [ ] フィード `?feed=following` 切替 (未ログインで `?feed=following` 指定 → all にフォールバック)

### ダークモード (Phase 2 / v0.3 追加)
- [ ] ヘッダーのトグルクリック → 全画面ダーク反映 / `localStorage.darkMode` 永続化
- [ ] リロードで設定保持
- [ ] OS 設定 (prefers-color-scheme) を初回起動時に反映

### 画像 (S3 統合 / Phase 3)
- [ ] 6 枚アップロード → 「最大 5 枚まで」エラー
- [ ] 6MB 画像 → 「5MB 以下」エラー
- [ ] アップロード成功 → S3 (本番) / local disk (dev) に保存 / 詳細画面で表示

### 派生集計値の権限ガード (F-LEAK-01 / Phase 2 / v0.3 追加)
- [ ] 他人の trip 詳細で `planned_count` / `budget_total` 等の派生フィールドが**自分のみ可視**になっている (`is_owner ? value : nil` / §12-5)
- [ ] 非 owner のレスポンス JSON に `planned_count: null` が含まれる (数値が漏れない)

### XSS / 入力
- [ ] title に `<script>alert(1)</script>` → エスケープ表示 (alert 出ない)
- [ ] コメント本文の Markdown レンダリング (該当する場合) で `<script>` 無害化

### セキュリティ (DevTools)
- [ ] Network パネルで Request Headers に **Authorization が露出していない** (Cookie 認証のため)
- [ ] Application パネルで Cookie の `HttpOnly` フラグが ON
- [ ] login を 5 回連続で誤入力 → **rack-attack で 429 Too Many Requests** (E-M1)
- [ ] logout 後の旧 token で API call → 401 (E-M2 RevokedJti)
