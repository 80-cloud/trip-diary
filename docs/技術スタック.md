# 旅行記録アプリ (trip-diary) 技術スタック

## 改訂履歴

| 版 | 日付 | 改訂者 | 内容 |
|---|---|---|---|
| 0.1 | 2026-05-17 | hideharu-AI | 初版 |

---

## 関連ドキュメント

- [要件定義書.md](./要件定義書.md)
- [インフラ構成.md](./インフラ構成.md)

---

## 1. 全体構成図

```
┌───────────┐    HTTPS (Phase3) / HTTP (local)
│  Browser  │ ──────────────────────────────────┐
└───────────┘                                   │
      ▲                                         ▼
      │ Nuxt 4 (SSR/SPA)                ┌──────────────────┐
      │ Tailwind / Pinia                │  Nginx (Phase3)  │
      │ Port: 3011 (local)              └────────┬─────────┘
      │                                          │
      │   ─────────────────────────────────────  │
      │                                          ▼
      │                                  ┌──────────────────┐
      │                                  │  Rails 8.1 API   │
      └──────────── fetch ──────────────►│  (Puma)          │
                       JWT in Cookie     │  Port: 3010      │
                                         └────────┬─────────┘
                                                  │ ActiveRecord
                                                  ▼
                                         ┌──────────────────┐
                                         │  MySQL 8         │
                                         │  Port: 3316 (local) │
                                         └──────────────────┘
                                                  │
                                         ┌──────────────────┐
                                         │ ActiveStorage    │
                                         │ Disk(Phase1) / S3(Phase3) │
                                         └──────────────────┘
```

---

## 2. レイヤー別採用技術

### 2-1. バックエンド

| 項目 | 採用技術 | バージョン | 備考 |
|---|---|---|---|
| 言語 | Ruby | 3.4.9 | rbenv 管理 |
| Web フレームワーク | Ruby on Rails | 8.1.x | API モード (`rails new --api`) |
| アプリサーバ | Puma | 6.x | Rails 同梱 |
| ORM | ActiveRecord | (Rails 同梱) | - |
| DB | MySQL | 8.x | Docker (`mysql:8.0`) |
| 認証 | JWT (`ruby-jwt`) | 2.x | HttpOnly Cookie に保持 |
| パスワード | BCrypt | 3.x | `has_secure_password` |
| ファイル | ActiveStorage | (Rails 同梱) | Disk (Phase1) / S3 (Phase3) |
| CORS | rack-cors | 2.x | Nuxt との同オリジン回避 |
| テスト | Minitest | (Rails 同梱) | |
| Linter | RuboCop | 1.x | Phase2 で導入 |

### 2-2. フロントエンド

| 項目 | 採用技術 | バージョン | 備考 |
|---|---|---|---|
| ランタイム | Node.js | 22 以上 | (本環境は v25 でも動作可) |
| フレームワーク | Nuxt | 4.x | Vue 3 ベース |
| 言語 | **JavaScript (純 JS)** | ES2024 | **TypeScript 不採用** (学習負荷軽減) |
| 状態管理 | Pinia | 3.x | 認証ユーザー保持 |
| ルーティング | Nuxt 標準 (`pages/`) | - | - |
| CSS | Tailwind CSS | 3.x (Nuxt module) | - |
| UI コンポーネント | @headlessui/vue / @heroicons/vue | 最新 | recipe-board と同じ |
| HTTP | `$fetch` (Nuxt built-in) | - | Cookie ベース認証 |
| バリデーション | 手書き (Phase2 で zod or vee-validate) | - | |
| テスト | (Phase3 で Playwright) | - | |

### 2-3. インフラ (Phase1: ローカル / Phase3: AWS)

| 項目 | Phase1 (local) | Phase3 (AWS) |
|---|---|---|
| OS | macOS / Linux | Amazon Linux 2023 |
| コンテナ | Docker (DB のみ) | (本番は EC2 上で puma 直起動) |
| Web サーバ | (Nuxt dev / Rails s) | Nginx (リバプロ) |
| DB | MySQL on Docker | RDS (db.t3.micro) |
| 画像 | ActiveStorage Disk | S3 |
| IaC | - | Terraform |
| ドメイン | localhost | (取得は任意 / IP 直叩きでも可) |
| 証明書 | - | Let's Encrypt (Phase3 検討) |

### 2-4. 開発支援

| 項目 | ツール |
|---|---|
| AI ペアプロ | Claude Code (講師推奨) |
| バージョン管理 | Git / GitHub (80-cloud/trip-diary) |
| Issue / PR テンプレ | `.github/` 配下 |
| エディタ | Cursor / VSCode |
| ブラウザ動作確認 | Chrome DevTools (Claude MCP 経由可) |

---

## 3. ポート割当 (recipe-board / sns-board と同時起動可能)

| サービス | ローカルポート | 競合回避先 |
|---|---|---|
| trip-diary Rails API | **3010** | recipe-board: 3000 |
| trip-diary Nuxt | **3011** | recipe-board: 3001 / sns-board: 5174 |
| trip-diary MySQL | **3316** | recipe-board: 3306 / sns-board PG: 5433 |

---

## 4. 採用しない技術と理由

| 候補技術 | 不採用の理由 |
|---|---|
| TypeScript | 学習段階 (習う→慣れる) で型定義に時間を取られると本筋が止まる。Phase4 で導入余地あり |
| GraphQL | REST で十分な規模。Rails の標準と相性が良い REST を選択 |
| Redis | キャッシュ / セッション要件が Phase 1〜2 では不要 |
| Sidekiq (非同期 Job) | 同期処理で十分。Phase3 で通知の非同期化を検討 |
| Docker (Rails / Nuxt) | 開発体験 (rbenv / npm の即時反映) を優先。DB のみ Docker |

---

## 5. ライセンス / OSS

- 本プロジェクトは学習目的・スクール提出物。コードは MIT ライセンスで公開予定
- 各依存パッケージのライセンスは `LICENSE-3rd-party` (Phase3) で集約
