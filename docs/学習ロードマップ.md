# 学習ロードマップ — 「習う → 慣れる → マスター」

## 改訂履歴

| 版 | 日付 | 改訂者 | 内容 |
|---|---|---|---|
| 0.1 | 2026-05-17 | hideharu-AI | 初版 (Phase別の学習姿勢と振り返り観点を定義) |

---

## 関連ドキュメント

- [要件定義書.md](./要件定義書.md) §1-2 学習到達点
- [機能一覧.md](./機能一覧.md)

---

## 0. このドキュメントの目的

本プロジェクト (trip-diary) の学習目的は「動くものを作る」ことだけでなく、**「なぜこの設計を選んだか」を自分の言葉で説明できる水準に到達すること**にある。
そのための学習段階を「**習う → 慣れる → マスター**」の 3 段階に整理し、各 Phase でどの姿勢で取り組むかを明文化する。

---

## 1. 3 段階の定義

| 段階 | 姿勢 | 行動指針 | 完了の目安 |
|---|---|---|---|
| **習う** | お手本に従う | recipe-board / sns-board / Rails ガイド / Nuxt 公式チュートを写経 | 自分一人で `rails new` → `bin/rails s` → ブラウザに表示 が止まらずできる |
| **慣れる** | 動かす | 完璧主義で止まらない。バグはあとで直す。一連の流れ (DB→API→UI) を 1 周回す | Phase 1 MVP が動く |
| **マスター** | 言語化する | 「なぜこの設計か」「他の選択肢は何か」「事故の芽はどこか」を口頭/文書で説明 | docs に振り返り (learning-notes) を書き、PR にレビューを受ける |

---

## 2. Phase × 段階 マッピング

| Phase | 主な段階 | 重点 |
|---|---|---|
| Phase 1 (MVP) | 習う / 慣れる | 写経 + 一連の流れの体感。**完璧主義で止まらない** |
| Phase 2 (発展機能) | 慣れる / マスター | 設計判断を意識し始める。コードレビュー前に「なぜこう書いたか」を言語化 |
| Phase 3 (AWS デプロイ) | マスター | 本番固有の事故 (SG / IAM / ログ漏れ) を経験。Runbook (`OPERATIONS.md`) を書く |
| Phase 4 (発展余地) | マスター | リアルタイム / PWA / 多言語 — 各機能の「なぜ必要か」を判断 |

---

## 3. Phase 1 で守りたい姿勢 (本セッション)

1. **動くを最優先**。Lint / 型 / テストの完璧さは Phase 2 へ持ち越して良い
2. ただし**機密情報 (`.env` / SECRET) は最初から守る**。後から対応は事故の元
3. **写経した箇所には「参考: recipe-board の XX」と PR / 学習ノートにメモを残す** — マスター段階で振り返るため
4. つまずいたら **Claude Code に「概念を簡単な言葉で説明して」と聞く**。原理を 1 つずつ吸収

---

## 4. マスター段階で問う観点 (振り返りチェックリスト)

実装した各機能について以下の問いに答えられるか確認:

### 4-1. 認証 (JWT in HttpOnly Cookie)

- なぜ localStorage ではなく HttpOnly Cookie に保存したか？
- CSRF はどう対策しているか？SameSite=Lax の限界は？
- access + refresh の 2 段方式に拡張するとしたら、どこを変える？

### 4-2. 旅行記録 CRUD + DayEntry (ネスト)

- `accepts_nested_attributes_for` を使う利点 / 欠点は？
- フォームから子要素を 1 件削除するときの ID 渡し方は？
- N+1 をどこで防いだか？(`includes(:day_entries)` の入れ忘れ事故)

### 4-3. コメント・いいね (counter_cache)

- カウンタが実数とズレるシナリオは？(例: race condition)
- 1 人 1 記録 1 いいねを保証する仕組みは？(DB UNIQUE / アプリ層 validation の二重防衛)

### 4-4. 画像アップロード (ActiveStorage)

- Disk と S3 を切り替えるとき、どの設定を変えるだけで済むか？
- 5MB 制限はどこで効かせているか？(クライアント / Rails / Web サーバ の三層)

### 4-5. 認可 (自分のデータのみ編集削除)

- 認可漏れが発生しやすい場所は？(`update` / `destroy` のリソース取り違え)
- before_action と Pundit / CanCanCan の違いは？

---

## 5. 振り返りログ (随時追記)

> 本セクションには、実装中に気づいたこと・つまずいたこと・解決方法を時系列で書き残す。

| 日付 | Phase | 気づき / トラブル | 解決 |
|---|---|---|---|
| 2026-05-17 | Phase 1 立ち上げ | recipe-board をベースに技術スタックを流用。**TypeScript を抜くだけで Nuxt の `tsconfig.json` を作らない手順**が必要 | `frontend/package.json` から `@types/*` を外し、設定ファイルは `nuxt.config.js` で記述 |
| 2026-05-17 | Phase 1 認証 | Rails API モードのデフォルトでは `ActionDispatch::Cookies` が外れており、`cookies.encrypted[...] = ...` で `Set-Cookie` が出ない | `config/application.rb` に `config.middleware.use ActionDispatch::Cookies` を明示的に追加 |
| 2026-05-17 | Phase 1 認証 | `JsonWebToken.encode(user_id: id)` がキーワード引数として扱われ `ArgumentError` | 呼び出し側で `encode({ user_id: id })` と明示的にハッシュを渡す |
| 2026-05-17 | Phase 1 フロント | Nuxt 4 では `app.vue` は `app/app.vue` に置く (`srcDir` が `app/` に変更されたため)。ルート直下に置くと `No entry found in rollupOptions.input` で起動失敗 | `frontend/app/app.vue` に配置 |
| 2026-05-17 | Phase 1 認証 | SSR モード (`ssr: true`) でブラウザ Cookie が Nitro サーバへ渡らず、`useAsyncData` で叩く `/me` が常に未認証になる | `nuxt.config.js` で `ssr: false` (SPA モード) に変更。Phase 2 で SSR + Cookie forwarding を検討 |
| 2026-05-17 | Phase 1 認証 | レイアウト `<script setup>` で `await auth.fetchMe()` を top-level await すると、ハードナビゲーション後に Pinia state が初期化前に評価され、ヘッダーが未ログイン表示でフリッカーする | `onMounted` で fetch + Nuxt plugin (client) で先に awaitすることで安定化 |

---

## 6. 参考リンク

- [Rails Guides — API モード](https://guides.rubyonrails.org/api_app.html)
- [Nuxt 4 公式](https://nuxt.com/)
- [ActiveStorage Overview](https://guides.rubyonrails.org/active_storage_overview.html)
- 姉妹プロジェクト
  - [recipe-board](https://github.com/80-cloud/recipe-board)
  - sns-board (要件定義書 / 機能一覧 の体系を踏襲)
